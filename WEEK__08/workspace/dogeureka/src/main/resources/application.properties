spring.application.name=dogeureka

#This is the default eureka port:
server.port=8761

#These are only necessary to disable HA and make
# our eureka server a single point of failure:
eureka.client.fetch-registry=false
eureka.client.register-with-eureka=false
eureka.server.enable-self-preservation=false
